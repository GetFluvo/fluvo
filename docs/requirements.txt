shibuya==2026.1.9
sphinx==9.1.0
sphinx-click==6.2.0
myst_parser==5.1.0
sphinx-copybutton==0.5.2
sphinx-mermaid
# Pygments 2.20 html-escapes the `linespans` option, but shibuya's
# WrapLineFormatter passes it as the int 1 -> AttributeError on build.
# Pin below 2.20 until shibuya passes a string. See GetFluvo/fluvo docs build.
pygments<2.20
